# IDEMMO asset map

Use the bundled `assets/` directory as the portable source of truth.

- `idemmo-logo.png` — approved IDEMMO logo
- `post-brand-kit-example.jpeg` — approved reference composition

When a customer clone provides a private `brand-assets/idemmo/` directory, inspect it for newer campaign-specific source files without copying customer materials into this distributable skill. Prefer supplied logos and house icons over recreating them. Keep campaign source files and renders in the customer project, not in the skill.
