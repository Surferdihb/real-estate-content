# Audit evidence checklist

Record the inspected master video, preview image, editable HTML, logo files, and output folders. Use `ffprobe` for MP4 dimensions/duration and inspect representative frames with an image viewer. A content audit is not complete if it checks only source code or only one campaign day.
